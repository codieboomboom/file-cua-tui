# FILE SERVER
This fileserver implementation allows a client to:
- List all files/subdirectory in a path (on server) with `/list` endpoint
- Read a file content given request path via `/read`
- get Info/Metadata of file using `/info`

## Run
To start the server:
`python3 fileserver.py`